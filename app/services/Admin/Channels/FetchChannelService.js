const moment = require("moment");

const Channels = require("../../../models/Channels");

const rules = {};

const errors = {};

const fetchChannel = async body => {};

module.exports = {
  rules,
  fetchChannel,
};
