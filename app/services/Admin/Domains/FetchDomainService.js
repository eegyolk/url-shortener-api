const moment = require("moment");

const Domains = require("../../../models/Domains");

const rules = {};

const errors = {};

const fetchDomain = async body => {};

module.exports = {
  rules,
  fetchDomain,
};
