const moment = require("moment");

const Links = require("../../../models/Links");

const rules = {};

const errors = {};

const fetchLink = async body => {};

module.exports = {
  rules,
  fetchLink,
};
