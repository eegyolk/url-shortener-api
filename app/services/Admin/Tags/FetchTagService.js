const moment = require("moment");

const Tags = require("../../../models/Tags");

const rules = {};

const errors = {};

const fetchTag = async body => {};

module.exports = {
  rules,
  fetchTag,
};
